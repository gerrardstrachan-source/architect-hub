# H2 v0.3 Provenance Note

The v0.3 world repairs the training action-marginal confound identified in the adversarial review. The repair substitutes training states `001` and `100` for `010` and `101`, producing identical H-S/H-C action marginals of 64 A0 and 128 A1 while preserving identical state exposure, order, trial count, and balanced feature marginals.

A separate local CSV materialization was independently validated at 1,152 rows. During repository transfer, an attempted CSV repository file was recognized as header-only and must not be treated as an experimental fixture. The repository state-sequence artifact is therefore the canonical committed fixture representation; the full CSV remains a local generated artifact only.

No ACT-R prediction, implementation, fixture freeze, or execution is authorized.
